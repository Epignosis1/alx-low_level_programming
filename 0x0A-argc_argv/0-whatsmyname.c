#include<stdio.h>
/**
 * main- a program that prints its name followed by a new line
 * @argc: counts of argument in command line
 * @argv: shows the strings
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	
	return(0);
}
