#include<stdio.h>
#include<string.h>
#include "main.h"
/**
 * _strlen - a function that returns the length of a string
 * @s: the string pointer
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
